@model MinecControlReport.Models.menuRole

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>menuRole</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.MenuRefId, "menu")
        </div>
        <div class="editor-field">
            @Html.DropDownList("MenuRefId", String.Empty)
            @Html.ValidationMessageFor(model => model.MenuRefId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.RolesRef)
        </div>
        <div class="editor-field">
            
            @Html.DropDownList("RolesRef", ViewBag.Roles as SelectList)
            @Html.ValidationMessageFor(model => model.RolesRef)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
