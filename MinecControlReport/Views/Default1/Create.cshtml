@model MinecControlReport.Models.kpis

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>kpis</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.KpiName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.KpiName)
            @Html.ValidationMessageFor(model => model.KpiName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.KpiStatus)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.KpiStatus)
            @Html.ValidationMessageFor(model => model.KpiStatus)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
